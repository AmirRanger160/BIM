# 🎛️ پنل مدیریت (Admin Panel)

پنل مدیریتی قدرتمند برای کنترل و مدیریت تمامی محتوای سایت BIM.

## 🚀 ویژگی‌ها

### 📝 مدیریت مقالات
- ایجاد، ویرایش و حذف مقالات
- مدیریت دسته‌بندی‌ها و برچسب‌ها
- آپلود تصاویر شاخص
- پیش‌نمایش محتوا

### 🎨 مدیریت گالری
- افزودن و ویرایش آیتم‌های گالری
- مدیریت دسته‌بندی‌ها
- نمایش اسلایدر تصاویر
- مدیریت متادیتا (مدت زمان، تاریخ، تکنولوژی‌ها)

### ⭐ مدیریت نظرات
- بررسی نظرات جدید
- تایید یا رد نظرات
- ویرایش یا حذف نظرات موجود
- مشاهده نظرات تایید نشده

### 📧 مدیریت پیام‌های تماس
- مشاهده تمام پیام‌های دریافت شده
- علامت‌گذاری پیام‌ها به عنوان خوانده شده
- حذف پیام‌های قدیمی
- مشاهده جزئیات کامل پیام

### 📊 داشبورد
- نمایش آمار سریع (مقالات، گالری، نظرات، پیام‌ها)
- نمایش پیام‌های خوانده‌نشده
- نمایش نظرات در انتظار تایید
- دسترسی سریع به عملیات رایج

## 🔐 امنیت

- احراز هویت و مجوز (Authentication & Authorization)
- توکن‌های JWT برای دسترسی محفوظ
- محدود کردن دسترسی به کاربران ادمین تنها
- حفاظت از روت‌های ادمین با Route Guards

## 🛣️ مسیرهای در دسترس

| مسیر | توضیح |
|------|--------|
| `/admin/login` | صفحه ورود ادمین |
| `/admin/dashboard` | داشبورد اصلی |
| `/admin/articles` | مدیریت مقالات |
| `/admin/gallery` | مدیریت گالری |
| `/admin/testimonials` | مدیریت نظرات |
| `/admin/contacts` | مدیریت پیام‌های تماس |

## 🔌 API Endpoints

### احراز هویت
```
POST /api/auth/login
```

### مقالات
```
GET    /api/admin/articles              - دریافت تمام مقالات
POST   /api/admin/articles              - ایجاد مقاله
PUT    /api/admin/articles/{id}         - ویرایش مقاله
DELETE /api/admin/articles/{id}         - حذف مقاله
```

### گالری
```
GET    /api/admin/gallery               - دریافت تمام آیتم‌ها
POST   /api/admin/gallery               - ایجاد آیتم
PUT    /api/admin/gallery/{id}          - ویرایش آیتم
DELETE /api/admin/gallery/{id}          - حذف آیتم
```

### نظرات
```
GET    /api/admin/testimonials          - دریافت تمام نظرات
POST   /api/admin/testimonials          - ایجاد نظر
PUT    /api/admin/testimonials/{id}     - ویرایش نظر
DELETE /api/admin/testimonials/{id}     - حذف نظر
PATCH  /api/admin/testimonials/{id}/approve - تایید نظر
```

### پیام‌های تماس
```
GET    /api/admin/contacts              - دریافت تمام پیام‌ها
PATCH  /api/admin/contacts/{id}/read    - علامت‌گذاری خوانده شده
DELETE /api/admin/contacts/{id}         - حذف پیام
```

### داشبورد
```
GET    /api/admin/dashboard/stats       - دریافت آمار داشبورد
```

## 📝 مثال استفاده

### ورود به پنل
```javascript
const response = await adminService.login({
  email: 'admin@example.com',
  password: 'password123'
})

// توکن ذخیره می‌شود در localStorage
localStorage.setItem('admin_token', response.access_token)
```

### دریافت آمار
```javascript
const stats = await adminService.getDashboardStats()
console.log(stats)
// {
//   articles: 10,
//   gallery: 25,
//   testimonials: 15,
//   contacts: 42,
//   unread_contacts: 5,
//   pending_testimonials: 3
// }
```

### ایجاد مقاله
```javascript
const article = await adminService.createArticle({
  title: 'عنوان مقاله',
  excerpt: 'خلاصه مقاله',
  full_content: 'محتوای کامل...',
  category: 'تکنولوژی',
  author: 'نام نویسنده',
  image: 'url-image'
})
```

## 👨‍💼 کاربران ادمین پیش‌فرض

هنگام راه‌اندازی برنامه، یک کاربر ادمین پیش‌فرض با اطلاعات زیر ایجاد می‌شود:

```
ایمیل: {ADMIN_EMAIL} (از .env)
رمز عبور: {ADMIN_PASSWORD} (از .env)
```

## 🎨 طراحی رابط کاربری

- رابط کاربری مدرن و تمیز
- طراحی responsive برای تمام دستگاه‌ها
- رنگ‌های گرادیانتی جذاب
- انیمیشن‌ها و transitions نرم
- دایناموی مناسب برای فیدبک کاربر

## 🔧 نیازمندی‌ها

- Vue 3
- Vue Router
- Axios
- FastAPI (بک‌اند)
- SQLAlchemy (دیتابیس)

## 📦 فایل‌های مربوط

### فرانت‌اند
- `/src/views/AdminLogin.vue` - صفحه ورود
- `/src/views/AdminDashboard.vue` - داشبورد
- `/src/views/AdminArticles.vue` - مدیریت مقالات
- `/src/views/AdminGallery.vue` - مدیریت گالری
- `/src/views/AdminTestimonials.vue` - مدیریت نظرات
- `/src/views/AdminContacts.vue` - مدیریت پیام‌ها
- `/src/components/AdminLayout.vue` - Layout اصلی
- `/src/api/services.js` - سرویس‌های API

### بک‌اند
- `/backend/app/routes/admin.py` - روت‌های مدیریتی

## 🚀 نحوه استفاده

1. به `/admin/login` بروید
2. اطلاعات ادمین را وارد کنید
3. داشبورد را باز کنید
4. از منوی سایدبار بین بخش‌های مختلف حرکت کنید
5. مقالات، گالری و نظرات را مدیریت کنید

## ⚠️ توجهات مهم

- توکن ادمین در localStorage ذخیره می‌شود
- در صورت خروج، توکن حذف می‌شود
- همه درخواست‌های API ادمین نیاز به توکن معتبر دارند
- فقط کاربران ادمین می‌توانند به این بخش دسترسی داشته باشند

## 📞 پشتیبانی

برای مشکلات یا سؤالات، لطفاً یک Issue ایجاد کنید.
