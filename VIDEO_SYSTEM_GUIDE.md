# راهنمای سیستم ویدیو 🎥

## ویژگی‌های پیاده‌شده:

### ۱. **پنل مدیریت ویدیو** (Admin Panel)
- مسیر: `/admin/videos`
- امکانات:
  - ✅ اضافه کردن ویدیوهای جدید
  - ✅ ویرایش ویدیوهای موجود
  - ✅ حذف ویدیوها
  - ✅ فعال/غیرفعال کردن ویدیوها
  - ✅ تغییر ترتیب نمایش

### ۲. **Modal پخش ویدیو** (Video Player Modal)
- پاپ‌آپ بزرگ برای نمایش ویدیو در صفحه اصلی
- پشتیبانی از:
  - ✅ YouTube (خودکار تشخیص و embed)
  - ✅ Vimeo (خودکار تشخیص و embed)
  - ✅ ویدیوهای مستقیم (MP4, WebM, OGG)
  - ✅ HTML embed شخصی‌سازی‌شده

### ۳. **دکمه ویدیو در صفحه اصلی** (Hero Section)
- دکمه "تماشای ویدیو" در بخش Hero
- خودکار بارگذاری اولین ویدیو فعال
- با کلیک بر روی دکمه، modal باز می‌شود

### ۴. **Backend API**
- مسیر: `/api/videos`
- Endpoints:
  - `GET /api/videos` - لیست ویدیوها (عمومی)
  - `GET /api/videos/{id}` - دریافت ویدیوی خاص
  - `POST /api/videos` - ایجاد ویدیو جدید (ادمین)
  - `PUT /api/videos/{id}` - بروزرسانی ویدیو (ادمین)
  - `DELETE /api/videos/{id}` - حذف ویدیو (ادمین)
  - `POST /api/videos/{id}/toggle` - فعال/غیرفعال کردن

---

## نحوه استفاده:

### در پنل ادمین:

1. **وارد شوید** به پنل ادمین
2. **از منوی ساید‌بار** گزینه "🎥 ویدیوها" را انتخاب کنید
3. **دکمه "➕ ویدیوی جدید"** را کلیک کنید
4. **فرم را تکمیل کنید:**
   - **عنوان**: نام ویدیو
   - **توضیح**: توضیح کوتاه
   - **لینک ویدیو**: URL ویدیو (YouTube, Vimeo, یا مستقیم)
   - **تصویر بند انگشتی** (اختیاری): تصویر پیش‌نمایش
   - **مدت زمان** (اختیاری): مثال "5:30"
   - **ترتیب نمایش**: شماره برای ترتیب
   - **فعال**: چک کنید برای فعال کردن

5. **دکمه "ایجاد"** یا **"بروزرسانی"** را کلیک کنید

### در صفحه اصلی:

1. اگر ویدیوی فعال وجود داشته باشد، **دکمه "▶ تماشای ویدیو"** نمایش داده می‌شود
2. **کلیک بر روی دکمه** modal بزرگ باز می‌شود
3. **ویدیو خودکار پخش می‌شود** با فیلم‌خان کامل
4. **دکمه بستن (✕)** یا کلیک خارج modal برای بستن

---

## فرمت‌های URL پشتیبانی‌شده:

### YouTube:
- ✅ `https://www.youtube.com/watch?v=dQw4w9WgXcQ`
- ✅ `https://youtu.be/dQw4w9WgXcQ`
- ✅ `https://www.youtube.com/embed/dQw4w9WgXcQ`

### Vimeo:
- ✅ `https://vimeo.com/123456789`
- ✅ `https://player.vimeo.com/video/123456789`

### ویدیو مستقیم:
- ✅ `https://example.com/video.mp4`
- ✅ `https://example.com/video.webm`
- ✅ `https://example.com/video.ogg`

### Custom HTML:
- ✅ کد HTML شخصی‌سازی‌شده

---

## اطلاعات دیتابیس:

### جدول Videos:
```sql
CREATE TABLE videos (
  id INTEGER PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  video_url VARCHAR(500) NOT NULL,
  thumbnail VARCHAR(500),
  duration VARCHAR(50),
  views INTEGER DEFAULT 0,
  active BOOLEAN DEFAULT TRUE,
  order INTEGER DEFAULT 0,
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)
```

---

## ویژگی‌های اضافی:

### تشخیص خودکار نوع ویدیو:
- سیستم خودکار نوع ویدیو را تشخیص می‌دهد
- URL های مختلف YouTube/Vimeo را به embed صحیح تبدیل می‌کند

### شمارش بازدید:
- هر بار که کسی ویدیو را ببیند، تعداد بازدید ۱ افزایش می‌یابد

### Responsive Design:
- پشتیبانی کامل برای تمام اندازه‌های صفحه
- Fullscreen support برای ویدیو

### Dark Mode:
- پنل ادمین در dark mode کاملاً عملکردی است

---

## نکات مهم:

⚠️ **لینک‌های YouTube:**
- برای YouTube معمولاً URL embed را استفاده کنید یا URL معمولی را وارد کنید - سیستم خودکار تبدیل می‌کند

⚠️ **تصویر بند انگشتی:**
- اگر تصویر وارد نکنید، یک گرادیان رنگی نمایش داده می‌شود

⚠️ **مدت زمان:**
- این فقط برای نمایش معلومات است - خودکار محاسبه نمی‌شود

---

## مثال کامل:

```json
{
  "title": "معرفی BIM",
  "description": "ویدیوی معرفی کوتاه درباره خدمات ما",
  "video_url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
  "thumbnail": "https://img.youtube.com/vi/dQw4w9WgXcQ/maxresdefault.jpg",
  "duration": "5:42",
  "active": true,
  "order": 0
}
```

---

🎉 **سیستم ویدیو آماده برای استفاده است!**
